<%= turbo_stream.append :foo do %>

  <% @posts.each do |post| %>
    <p>
      <%= post.id %>
    </p>
    <p>
      <%= post.title %>
    </p>
  <% end %>
<% end %>

<%= turbo_stream.replace :foo2 do %>
  <% if @pagy.next.present? %>
    <%= turbo_frame_tag :foo2, 
      loading: :lazy,
      src: posts3_index_path(format: :turbo_stream, page: @pagy.next)
    %>
  <% end %>
<% end %>
