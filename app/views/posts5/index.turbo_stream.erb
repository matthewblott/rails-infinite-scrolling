<%= turbo_stream.append :foo do %>
  <% @posts.each do |post| %>
    <tr is="turbo-frame">
      <td>
        <%= post.id %>
      <td>
      <td>
        <%= post.title %>
      </td>
    </tr>
  <% end %>
<% end %>

<%= turbo_stream.replace :foo2 do %>
  <% if @pagy.next.present? %>
    <%= turbo_frame_tag :foo2, 
      loading: :lazy,
      src: posts5_index_path(format: :turbo_stream, page: @pagy.next)
    %>
  <% end %>
<% end %>
